#include <stdio.h>
#include <stdlib.h>

/* Aqui conseguimos saber antes do programa executar a quantidade de dados que será utilizado pelo programa.
Mas há programas que não sabemos antes de executar, pra isso que serve a alocação donâmica*/

int main(){

    short x;
    x=10;

    int c[10];

    short *p = &x;


    return 0;
}
